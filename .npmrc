# Expo + pnpm monorepo: Metro expects flat node_modules. Hoist everything
# so transitives are resolvable from apps/mobile/node_modules + root
# node_modules without traversing the .pnpm/ virtual store.
public-hoist-pattern[]=*
shamefully-hoist=true
